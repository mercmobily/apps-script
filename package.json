{
  "scripts": {
    "script-push": "cd appsScript; clasp push",
    "build": "rimraf distr && rollup -c rollup.config.js",
    "buildPush": "npm run build; git add distr; git commit -m 'Built'; git push",
    "start": "cd appsScript; clasp open --webapp"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^0.15.1",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.4",
    "es-dev-server": "^1.5.0"
  },
  "name": "wattle-app",
  "version": "0.0.1",
  "description": "Webcomponent wattle-app following open-wc recommendations",
  "author": "wattle-app",
  "license": "MIT",
  "dependencies": {
    "lit-html": "^1.0.0",
    "lit-element": "^2.0.1"
  }
}
